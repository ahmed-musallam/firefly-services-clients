/**
 * Generated by orval v7.14.0 🍺
 * Do not edit manually.
 * Firefly Lightroom API Reference
 * Programmatically apply Lightroom settings and image adjustments to photos using Adobe's cloud-based image processing services
 * OpenAPI spec version: 1.0.0
 */
/**
 * The input assets for your call.
 */
export type PostPresetsSchemaInputs = {
  source: InputsSchema;
  /** @minItems 1 */
  presets: InputsSchema[];
};

/**
 * Initiates an asynchronous job to apply a preset on an image.
 */
export interface PostPresetsSchema {
  /** The input assets for your call. */
  inputs: PostPresetsSchemaInputs;
  outputs: OutputsSchema;
}

/**
 * The input assets for your call.
 */
export type PostXmpSchemaInputs = {
  source: InputsSchema;
  xmp: InputsSchema;
};

/**
 * Initiates an asynchronous job to apply an XMP setting to an image.
 */
export interface PostXmpSchema {
  /** The input assets for your call. */
  inputs: PostXmpSchemaInputs;
  outputs: OutputsSchema;
}

/**
 * Initiates an asynchronous job to auto straighten an image.
 */
export interface PostAutoStraightenSchema {
  inputs: InputsSchema;
  outputs: OutputsSchema;
}

/**
 * Initiates an asynchronous job to apply Auto Tone to an image.
 */
export interface PostAutoToneSchema {
  inputs: InputsSchema;
  outputs: OutputsSchema;
}

/**
 * The input assets for your call.
 */
export type PostImageEditSchemaInputs = {
  source: InputsSchema;
  edits?: InputsSchema;
};

/**
 * Enables programmatic adjustment of values within the Lightroom Edit Panel and applies them to an image.
 */
export interface PostImageEditSchema {
  /** The input assets for your call. */
  inputs: PostImageEditSchemaInputs;
  outputs: OutputsSchema;
}

/**
 * The current status of the job.
 */
export type GetStatusSchemaStatus =
  (typeof GetStatusSchemaStatus)[keyof typeof GetStatusSchemaStatus];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetStatusSchemaStatus = {
  pending: 'pending',
  running: 'running',
  succeeded: 'succeeded',
  failed: 'failed',
} as const;

/**
 * Error details if job failed.
 */
export type GetStatusSchemaError = {
  /** Error code. */
  code?: string;
  /** Error message. */
  message?: string;
};

/**
 * Returns the status of a job.
 */
export interface GetStatusSchema {
  /** The job identifier. */
  jobId?: string;
  /** The current status of the job. */
  status?: GetStatusSchemaStatus;
  /** When the job was created. */
  created?: string;
  /** When the job was last modified. */
  modified?: string;
  /** Output assets when job is completed. */
  outputs?: OutputsSchema[];
  /** Error details if job failed. */
  error?: GetStatusSchemaError;
}

export type _ApiResponseSuccessSchemaLinksSelf = {
  /** The link that client can use to get status of the asynchronous job. */
  href?: string;
};

/**
 * _links hrefs the client can use to get status of the asynchronous job.
 */
export type _ApiResponseSuccessSchemaLinks = {
  self?: _ApiResponseSuccessSchemaLinksSelf;
};

/**
 * Provides API response.
 */
export interface ApiResponseSuccessSchema {
  /** _links hrefs the client can use to get status of the asynchronous job. */
  _links?: _ApiResponseSuccessSchemaLinks;
}

/**
 * Further details of the error.
 */
export type ApiResponse400ErrorSchemaDetails = {
  /** The name for the detailed error. */
  name?: string;
  /** An explanation of the error. */
  reason?: string;
};

/**
 * Provides API response.
 */
export interface ApiResponse400ErrorSchema {
  /** A summary of the error. */
  title?: string;
  /** The error type. */
  type?: string;
  /** The error code. */
  code?: string;
  /** Further details of the error. */
  details?: ApiResponse400ErrorSchemaDetails;
}

/**
 * Further details of the error.
 */
export type ApiResponse402ErrorSchemaDetails = {
  /** The name for the detailed error. */
  name?: string;
  /** An explanation of the error. */
  reason?: string;
};

/**
 * Provides API response.
 */
export interface ApiResponse402ErrorSchema {
  /** A summary of the error. */
  title?: string;
  /** The error type. */
  type?: string;
  /** The error code. */
  code?: string;
  /** Further details of the error. */
  details?: ApiResponse402ErrorSchemaDetails;
}

/**
 * Further details of the error.
 */
export type ApiResponse403ErrorSchemaDetails = {
  /** The name for the detailed error. */
  name?: string;
  /** An explanation of the error. */
  reason?: string;
};

/**
 * Provides API response.
 */
export interface ApiResponse403ErrorSchema {
  /** A summary of the error. */
  title?: string;
  /** The error type. */
  type?: string;
  /** The error code. */
  code?: string;
  /** Further details of the error. */
  details?: ApiResponse403ErrorSchemaDetails;
}

/**
 * Further details of the error.
 */
export type ApiResponse404ErrorSchemaDetails = {
  /** The name for the detailed error. */
  name?: string;
  /** An explanation of the error. */
  reason?: string;
};

/**
 * Provides API response.
 */
export interface ApiResponse404ErrorSchema {
  /** A summary of the error. */
  title?: string;
  /** The error type. */
  type?: string;
  /** The error code. */
  code?: string;
  /** Further details of the error. */
  details?: ApiResponse404ErrorSchemaDetails;
}

/**
 * Further details of the error.
 */
export type ApiResponse409ErrorSchemaDetails = {
  /** The name for the detailed error. */
  name?: string;
  /** An explanation of the error. */
  reason?: string;
};

/**
 * Provides API response.
 */
export interface ApiResponse409ErrorSchema {
  /** A summary of the error. */
  title?: string;
  /** The error type. */
  type?: string;
  /** The error code. */
  code?: string;
  /** Further details of the error. */
  details?: ApiResponse409ErrorSchemaDetails;
}

/**
 * Further descriptions of the exact error where details is substituted for a specific issue.
 */
export type ApiResponse410ErrorSchemaDetails = {
  /** Name for the detailed error. */
  name?: string;
  /** Details of error reason. */
  reason?: string;
};

/**
 * Provides API response.
 */
export interface ApiResponse410ErrorSchema {
  /** A short, human-readable summary of the problem. */
  title?: string;
  /** A machine-readable error type. */
  type?: string;
  /** A machine-readable error code. */
  code?: string;
  /** Further descriptions of the exact error where details is substituted for a specific issue. */
  details?: ApiResponse410ErrorSchemaDetails;
}

/**
 * Input asset definition.
 */
export interface InputsSchema {
  /** URL or pre-signed URL of the input asset. */
  href: string;
  /** Storage type for the input asset. */
  storage: string;
}

export type OutputsSchemaItem = {
  /** Pre-signed URL where the output asset is stored or can be accessed. */
  href: string;
  /** Storage type for the output asset. */
  storage: string;
};

/**
 * Output asset definition.
 */
export type OutputsSchema = OutputsSchemaItem[];

export type AcrstatusPathParameters = {
  jobId: string;
};
