/**
 * Generated by orval v7.14.0 🍺
 * Do not edit manually.
 * Firefly Services Audio and Video API
 * This RESTful API provides resources to use Firefly Services AI for audio and video generation and processing.
 * OpenAPI spec version: 1.0.0
 */
/**
 * List of available voices.
 */
export interface SuccessfulVoicesResponse {
  voices?: VoiceEntity[];
}

/**
 * The speaking style of the voice.
 */
export type VoiceEntityStyle = (typeof VoiceEntityStyle)[keyof typeof VoiceEntityStyle];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VoiceEntityStyle = {
  Casual: 'Casual',
  Excited: 'Excited',
} as const;

/**
 * This determines if the specific voice ID is available for use. Deprecated voices will have an Inactive status.
 */
export type VoiceEntityStatus = (typeof VoiceEntityStatus)[keyof typeof VoiceEntityStatus];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VoiceEntityStatus = {
  Active: 'Active',
  Inactive: 'Inactive',
} as const;

/**
 * This is a field for any additional properties for the voice.
 */
export type VoiceEntityExtendedPropertyMap = { [key: string]: unknown };

/**
 * Details of Voice Entity.
 */
export interface VoiceEntity {
  /** The unique ID for the voice. */
  voiceId?: string;
  /** The displayed name for the voice. */
  displayName?: string;
  /** The gender of the voice. */
  gender?: string;
  /** The speaking style of the voice. */
  style?: VoiceEntityStyle;
  /** Not applicable at this time. */
  voiceType?: string;
  /** This determines if the specific voice ID is available for use. Deprecated voices will have an Inactive status. */
  status?: VoiceEntityStatus;
  /** This is a field for any additional properties for the voice. */
  extendedPropertyMap?: VoiceEntityExtendedPropertyMap;
  /** The speed of speaking, in words per minute. */
  wordsPerMinute?: string;
  /** Not applicable at this time. */
  rolePlayList?: string[];
  /** A pre-signed URL for downloading the sample audio. */
  sampleURL?: string;
}

/**
 * List of available avatars.
 */
export interface SuccessfulAvatarsResponse {
  avatars?: AvatarEntity[];
}

/**
 * The clothing style worn by the avatar.
 */
export type AvatarEntityClothingStyle =
  (typeof AvatarEntityClothingStyle)[keyof typeof AvatarEntityClothingStyle];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AvatarEntityClothingStyle = {
  Casual: 'Casual',
  Formal: 'Formal',
  Professional: 'Professional',
} as const;

/**
 * The intended age bracket of the avatar.
 */
export type AvatarEntityAgeGroup = (typeof AvatarEntityAgeGroup)[keyof typeof AvatarEntityAgeGroup];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AvatarEntityAgeGroup = {
  Young_Adult: 'Young Adult',
  Middle_Aged: 'Middle Aged',
  Senior: 'Senior',
} as const;

/**
 * This determines if the specific avatar ID is available for use. Deprecated avatars will have an Inactive status.
 */
export type AvatarEntityStatus = (typeof AvatarEntityStatus)[keyof typeof AvatarEntityStatus];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AvatarEntityStatus = {
  Active: 'Active',
  Inactive: 'Inactive',
} as const;

export type AvatarEntityExtendedPropertyMapAspectRatio = {
  x?: number;
  y?: number;
};

export type AvatarEntityExtendedPropertyMapResolution = {
  width?: number;
  height?: number;
};

/**
 * This is a field for any additional properties for the avatar.
 */
export type AvatarEntityExtendedPropertyMap = {
  aspectRatio?: AvatarEntityExtendedPropertyMapAspectRatio;
  resolution?: AvatarEntityExtendedPropertyMapResolution;
};

export type AvatarEntityThumbnailUrls = {
  /** A pre-signed URL for downloading the high-resolution avatar thumbnail. */
  hd?: string;
  /** A pre-signed URL for downloading the low-resolution avatar thumbnail. */
  lowRes?: string;
};

export type AvatarEntitySampleVideo = {
  /** A pre-signed URL for downloading avatar sample video in WEBM format. */
  webm?: string;
  /** A pre-signed URL for downloading avatar sample video in MP4 format. */
  mp4?: string;
};

/**
 * Details of the avatar entity.
 */
export interface AvatarEntity {
  /** The unique ID for the avatar. */
  avatarId?: string;
  /** The displayed name for the avatar. */
  displayName?: string;
  /** The gender of the avatar. */
  gender?: string;
  /** The clothing style worn by the avatar. */
  clothingStyle?: AvatarEntityClothingStyle;
  /** The intended age bracket of the avatar. */
  ageGroup?: AvatarEntityAgeGroup;
  /** The ethnicity of the avatar. */
  ethnicity?: string;
  /** This property is used when the avatar wears a professional or region-specific costume (like Nurse, Doctor, Emergency Worker, Retail Worker, etc.). */
  style?: string;
  /** This determines if the specific avatar ID is available for use. Deprecated avatars will have an Inactive status. */
  status?: AvatarEntityStatus;
  /** This is a field for any additional properties for the avatar. */
  extendedPropertyMap?: AvatarEntityExtendedPropertyMap;
  thumbnailUrls?: AvatarEntityThumbnailUrls;
  /** The unique ID for the voice. */
  voiceId?: string;
  sampleVideo?: AvatarEntitySampleVideo;
}

/**
 * The language code for the supported language desired in the result.
 */
export type SupportedLanguageCode =
  (typeof SupportedLanguageCode)[keyof typeof SupportedLanguageCode];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SupportedLanguageCode = {
  'en-US': 'en-US',
  'es-ES': 'es-ES',
  'de-DE': 'de-DE',
  'fr-FR': 'fr-FR',
  'da-DK': 'da-DK',
  'en-GB': 'en-GB',
  'en-IN': 'en-IN',
  'es-419': 'es-419',
  'es-AR': 'es-AR',
  'fr-CA': 'fr-CA',
  'hi-IN': 'hi-IN',
  'it-IT': 'it-IT',
  'ja-JP': 'ja-JP',
  'ko-KR': 'ko-KR',
  'nb-NO': 'nb-NO',
  'pt-BR': 'pt-BR',
  'pt-PT': 'pt-PT',
  'nl-NL': 'nl-NL',
  'zh-CN': 'zh-CN',
  'sv-SE': 'sv-SE',
} as const;

/**
 * The supported audio format for output.
 */
export type SupportedAudioOutputAudioFormat =
  (typeof SupportedAudioOutputAudioFormat)[keyof typeof SupportedAudioOutputAudioFormat];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SupportedAudioOutputAudioFormat = {
  'audio/wav': 'audio/wav',
} as const;

/**
 * The supported video format for background video for Avatar.
 */
export type SupportedOutputVideoFormat =
  (typeof SupportedOutputVideoFormat)[keyof typeof SupportedOutputVideoFormat];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SupportedOutputVideoFormat = {
  'video/mp4': 'video/mp4',
} as const;

/**
 * Supported background type for output Avatar video.
 */
export type SupportedBackgroundType =
  (typeof SupportedBackgroundType)[keyof typeof SupportedBackgroundType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SupportedBackgroundType = {
  color: 'color',
  video: 'video',
  image: 'image',
} as const;

export type TTSRequest = GenerateSpeechUsingPlainText;

/**
 * The format of the text resource.
 */
export type GenerateSpeechUsingPlainTextScriptMediaType =
  (typeof GenerateSpeechUsingPlainTextScriptMediaType)[keyof typeof GenerateSpeechUsingPlainTextScriptMediaType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GenerateSpeechUsingPlainTextScriptMediaType = {
  'text/plain': 'text/plain',
} as const;

export type GenerateSpeechUsingPlainTextScript =
  | (TextSource & {
      localeCode: SupportedLanguageCode;
      /** The format of the text resource. */
      mediaType: GenerateSpeechUsingPlainTextScriptMediaType;
    })
  | (URLSource & {
      localeCode: SupportedLanguageCode;
      /** The format of the text resource. */
      mediaType: GenerateSpeechUsingPlainTextScriptMediaType;
    });

export type GenerateSpeechUsingPlainTextOutput = {
  mediaType: SupportedAudioOutputAudioFormat;
};

export interface GenerateSpeechUsingPlainText {
  script: GenerateSpeechUsingPlainTextScript;
  /** ID of the voice to use from the catalog of voices. */
  voiceId: string;
  output: GenerateSpeechUsingPlainTextOutput;
}

export type AvatarRequest = GenerateAvatarUsingPlainText | GenerateAvatarUsingAudioFile;

/**
 * The format of the text resource.
 */
export type GenerateAvatarUsingPlainTextScriptMediaType =
  (typeof GenerateAvatarUsingPlainTextScriptMediaType)[keyof typeof GenerateAvatarUsingPlainTextScriptMediaType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GenerateAvatarUsingPlainTextScriptMediaType = {
  'text/plain': 'text/plain',
} as const;

export type GenerateAvatarUsingPlainTextScript =
  | (TextSource & {
      localeCode: SupportedLanguageCode;
      /** The format of the text resource. */
      mediaType: GenerateAvatarUsingPlainTextScriptMediaType;
    })
  | (URLSource & {
      localeCode: SupportedLanguageCode;
      /** The format of the text resource. */
      mediaType: GenerateAvatarUsingPlainTextScriptMediaType;
    });

export interface GenerateAvatarUsingPlainText {
  script: GenerateAvatarUsingPlainTextScript;
  /** ID of the voice to use from the catalog of voices. */
  voiceId: string;
  /** ID of the avatar to use from the catalog of avatars. */
  avatarId: string;
  output: AvatarOutputObject;
}

export type GenerateAvatarUsingAudioFileAudioMediaType =
  (typeof GenerateAvatarUsingAudioFileAudioMediaType)[keyof typeof GenerateAvatarUsingAudioFileAudioMediaType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GenerateAvatarUsingAudioFileAudioMediaType = {
  'audio/wav': 'audio/wav',
} as const;

export type GenerateAvatarUsingAudioFileAudio = {
  source: URLObject;
  mediaType: GenerateAvatarUsingAudioFileAudioMediaType;
  localeCode: SupportedLanguageCode;
};

export interface GenerateAvatarUsingAudioFile {
  audio: GenerateAvatarUsingAudioFileAudio;
  /** ID of the avatar to use from the catalog of avatars. */
  avatarId: string;
  output: AvatarOutputObject;
}

/**
 * Details of the source including a pre-signed URL. See the [list of allowed domains for storage](https://developer.adobe.com/audio-video-firefly-services/getting-started/storage-solutions/).
 */
export type URLSourceSource = {
  /** The pre-signed URL pointing to the resource to be used for avatar/speech generation. */
  url?: string;
};

export interface URLSource {
  /** Details of the source including a pre-signed URL. See the [list of allowed domains for storage](https://developer.adobe.com/audio-video-firefly-services/getting-started/storage-solutions/). */
  source?: URLSourceSource;
}

/**
 * URL object containing a pre-signed URL. See the [list of allowed domains for storage](https://developer.adobe.com/audio-video-firefly-services/getting-started/storage-solutions/).
 */
export interface URLObject {
  /** The pre-signed URL pointing to the resource to be used for avatar/speech generation. */
  url?: string;
}

export type AvatarOutputObjectBackground =
  | (OutputColor & {
      type?: SupportedBackgroundType;
    })
  | (URLSource & {
      type?: SupportedBackgroundType;
    });

export type AvatarOutputObjectResolution = {
  width?: string;
  height?: string;
};

/**
 * Output configuration for avatar video generation.
 */
export interface AvatarOutputObject {
  mediaType: SupportedOutputVideoFormat;
  background?: AvatarOutputObjectBackground;
  resolution?: AvatarOutputObjectResolution;
}

/**
 * Details of the text resource.
 */
export type TextSource = string;

/**
 * Color specification for avatar background.
 */
export type OutputColor = string;

/**
 * Response returned when a job is successfully submitted.
 */
export interface SubmitAPIResponse {
  jobId?: string;
  statusUrl?: string;
}

/**
 * Response object for job status queries.
 */
export type StatusAPIResponse =
  | StatusAPIInprogressResponse
  | StatusAPICompletedResponse
  | StatusAPIFailedResponse
  | JobStatus;

/**
 * Status of the job.
 */
export type StatusAPIResponseStatus =
  (typeof StatusAPIResponseStatus)[keyof typeof StatusAPIResponseStatus];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const StatusAPIResponseStatus = {
  pending: 'pending',
  running: 'running',
  failed: 'failed',
  succeeded: 'succeeded',
} as const;

export interface StatusAPIInprogressResponse {
  jobId?: string;
  status?: StatusAPIResponseStatus;
}

export type StatusAPICompletedResponseOutput = {
  destination?: Destination;
};

export interface StatusAPICompletedResponse {
  jobId?: string;
  status?: StatusAPIResponseStatus;
  output?: StatusAPICompletedResponseOutput;
}

/**
 * Response object for failed jobs.
 */
export interface StatusAPIFailedResponse {
  status?: StatusAPIResponseStatus;
  jobId?: string;
  error_code?: string;
  message?: string;
}

/**
 * Generic error response object.
 */
export interface FailedResponse {
  error_code?: string;
  message?: string;
}

export interface ErrorResponse {
  /** The error code returned with the response. */
  error_code: string;
  /** The error message returned with the response. */
  message: string;
  /** The error message ID returned with the response. */
  messageId?: string;
}

/**
 * Contains metadata about the source of the input template.
 */
export type TemplateDescribeRequestSource = {
  /** A pre-signed URL pointing to the input template file. */
  url: string;
};

export interface TemplateDescribeRequest {
  /** Contains metadata about the source of the input template. */
  source: TemplateDescribeRequestSource;
}

export interface TemplateDescribeResponse {
  /** Unique identifier for the describe job. */
  jobId: string;
  /** URL to poll for the status of the describe job. */
  statusUrl: string;
}

/**
 * Maximum frames per second as a fraction.
 */
export type PresetItemMaxFps = {
  /** Frames-per-second numerator. */
  numerator: number;
  /** Frames-per-second denominator. */
  denominator: number;
};

export interface PresetItem {
  /** Unique identifier for the preset. */
  presetId: string;
  /** Human-readable label for the preset. */
  label: string;
  /** MIME type of the output media (e.g., video/mp4). */
  mediaType: string;
  /** Video codec used (e.g., H.264). */
  codec: string;
  /** Codec profile (e.g., high). */
  profile?: string;
  /** Maximum frames per second as a fraction. */
  maxFps: PresetItemMaxFps;
  /** Bitrate mode (e.g., vbr, cbr). */
  bitrateMode: string;
  /** Target bitrate in kilobits per second. */
  targetBitrateInKbps?: number;
  /** Maximum bitrate in kilobits per second. */
  maxBitrateInKbps?: number;
  /** Whether the preset supports alpha channel. */
  alpha: boolean;
  /** Primary use case for this preset. */
  primaryUsage: string;
}

export interface PresetsResponse {
  items: PresetItem[];
}

/**
 * Contains metadata about the source of the input template.
 */
export type TemplateRenderRequestSource = {
  /** A pre-signed URL pointing to the input template file. */
  url: string;
};

export type TemplateRenderRequestFontsItemSource = {
  /** Pre-signed URL for the font file (.ttf/.otf). */
  url: string;
};

export type TemplateRenderRequestFontsItem = {
  /** PostScript name of the font. */
  name: string;
  source: TemplateRenderRequestFontsItemSource;
};

/**
 * How to handle missing fonts. 'fail' will cause the render to fail; 'use_default' will use Premiere Pro fallback behavior.
 */
export type TemplateRenderRequestConfigHandleMissingFonts =
  (typeof TemplateRenderRequestConfigHandleMissingFonts)[keyof typeof TemplateRenderRequestConfigHandleMissingFonts];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TemplateRenderRequestConfigHandleMissingFonts = {
  fail: 'fail',
  use_default: 'use_default',
} as const;

/**
 * Configuration options for the render request.
 */
export type TemplateRenderRequestConfig = {
  /** How to handle missing fonts. 'fail' will cause the render to fail; 'use_default' will use Premiere Pro fallback behavior. */
  handleMissingFonts?: TemplateRenderRequestConfigHandleMissingFonts;
};

export type TemplateRenderRequestAssetsItemSource = {
  /** Pre-signed URL for the image, video, or audio asset. */
  url: string;
};

export type TemplateRenderRequestAssetsItem = {
  source: TemplateRenderRequestAssetsItemSource;
};

export type TemplateRenderRequestPresetsItemSource = {
  /** ID of a built-in preset from the presets endpoint. */
  presetId?: string;
  /** Pre-signed URL for a custom Encoder preset file. */
  url?: string;
};

export type TemplateRenderRequestPresetsItem = {
  source: TemplateRenderRequestPresetsItemSource;
};

/**
 * Scaling behavior for media controls.
 */
export type TemplateRenderRequestVariationsItemVariablesItemScale =
  (typeof TemplateRenderRequestVariationsItemVariablesItemScale)[keyof typeof TemplateRenderRequestVariationsItemVariablesItemScale];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TemplateRenderRequestVariationsItemVariablesItemScale = {
  no_scale: 'no_scale',
  fit_to_frame: 'fit_to_frame',
  stretch_to_fill: 'stretch_to_fill',
  fill_frame: 'fill_frame',
} as const;

/**
 * Audio preference for audio controls.
 */
export type TemplateRenderRequestVariationsItemVariablesItemAudioPreference =
  (typeof TemplateRenderRequestVariationsItemVariablesItemAudioPreference)[keyof typeof TemplateRenderRequestVariationsItemVariablesItemAudioPreference];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TemplateRenderRequestVariationsItemVariablesItemAudioPreference = {
  replace: 'replace',
  mix: 'mix',
} as const;

export type TemplateRenderRequestVariationsItemVariablesItem = {
  /** Unique identifier for the template variable (from describe response). */
  variableId: string;
  /** Override for checkbox controls. */
  selectedCheckboxValue?: boolean;
  /** Override for dropdown controls. */
  selectedDropdownValue?: string;
  /** Override for slider controls. */
  selectedSliderValue?: number;
  /** Override for text controls. */
  text?: string;
  /** PostScript font name for text controls. */
  fontName?: string;
  /** Index into the assets array for media or audio controls. */
  assetIndex?: number;
  /** Scaling behavior for media controls. */
  scale?: TemplateRenderRequestVariationsItemVariablesItemScale;
  /** Audio preference for audio controls. */
  audioPreference?: TemplateRenderRequestVariationsItemVariablesItemAudioPreference;
};

export type TemplateRenderRequestVariationsItem = {
  /** Variable overrides for this variation. Each object targets one template variable by variableId and supplies the override value. */
  variables: TemplateRenderRequestVariationsItemVariablesItem[];
};

export type TemplateRenderRequestOutputsItem = {
  /** Zero-based index of the variation to render. */
  variationIndex: number;
  /** Zero-based index of the preset in the presets array. */
  presetIndex: number;
  /** Optional custom file name for the output. */
  fileName?: string;
};

export interface TemplateRenderRequest {
  /** Contains metadata about the source of the input template. */
  source: TemplateRenderRequestSource;
  /** Array of fonts to be used in the template. */
  fonts?: TemplateRenderRequestFontsItem[];
  /** Configuration options for the render request. */
  config?: TemplateRenderRequestConfig;
  /** Array of image, video, or audio assets referenced by variations via assetIndex. */
  assets?: TemplateRenderRequestAssetsItem[];
  /** Array of export presets. Each item is either a built-in preset (by presetId) or a custom preset (by URL). Outputs reference presets by presetIndex. */
  presets?: TemplateRenderRequestPresetsItem[];
  /**
   * List of variations. Each variation contains variable overrides applied to the template.
   * @minItems 1
   */
  variations: TemplateRenderRequestVariationsItem[];
  /** Defines which variation and preset combinations to render and optional output file names. */
  outputs?: TemplateRenderRequestOutputsItem[];
}

export interface TemplateRenderResponse {
  /** Unique identifier for the render job. */
  jobId: string;
  /** URL to poll for the status of the rendering job. */
  statusUrl: string;
}

/**
 * Current status of the job.
 */
export type JobStatusStatus = (typeof JobStatusStatus)[keyof typeof JobStatusStatus];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JobStatusStatus = {
  not_started: 'not_started',
  running: 'running',
  succeeded: 'succeeded',
  failed: 'failed',
  partially_succeeded: 'partially_succeeded',
} as const;

export type JobStatusOutputsItemDestination = {
  /** URL from which the rendered video can be downloaded. */
  url: string;
};

export type JobStatusOutputsItem = {
  /** Zero-based index of the variation that was rendered. */
  variationIndex: number;
  /** Zero-based index of the preset that was used for rendering. */
  presetIndex: number;
  destination: JobStatusOutputsItemDestination;
};

export type JobStatusErrorsItemError = {
  /** Error code for the failure. */
  error_code: string;
  /** Error message describing the failure. */
  message: string;
};

export type JobStatusErrorsItem = {
  /** Zero-based index of the variation that failed. */
  variationIndex: number;
  /** Zero-based index of the preset that was used for the failed variation. */
  presetIndex: number;
  error: JobStatusErrorsItemError;
};

export type JobStatusOutputFontsItem = {
  /** PostScript name of the font. */
  name: string;
  /** Whether the font needs to be uploaded in the render request. */
  uploadRequired?: boolean;
};

/**
 * Type of the element.
 */
export type JobStatusOutputElementsItemType =
  (typeof JobStatusOutputElementsItemType)[keyof typeof JobStatusOutputElementsItemType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JobStatusOutputElementsItemType = {
  mogrt: 'mogrt',
} as const;

/**
 * Type of the control.
 */
export type JobStatusOutputElementsItemControlsItemType =
  (typeof JobStatusOutputElementsItemControlsItemType)[keyof typeof JobStatusOutputElementsItemControlsItemType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JobStatusOutputElementsItemControlsItemType = {
  text: 'text',
  media: 'media',
  dropdown: 'dropdown',
  checkbox: 'checkbox',
  slider: 'slider',
  audio: 'audio',
  comment: 'comment',
} as const;

/**
 * Default or current values for the control (e.g., text, fontName, selectedCheckboxValue, selectedDropdownValue, selectedSliderValue, scale).
 */
export type JobStatusOutputElementsItemControlsItemDefaultData = { [key: string]: unknown };

/**
 * Data associated with the control.
 */
export type JobStatusOutputElementsItemControlsItemData = { [key: string]: unknown };

/**
 * Minimum and maximum values for slider controls.
 */
export type JobStatusOutputElementsItemControlsItemRange = {
  minimum?: number;
  maximum?: number;
};

/**
 * Dimensions for media controls.
 */
export type JobStatusOutputElementsItemControlsItemSize = {
  width?: number;
  height?: number;
};

export type JobStatusOutputElementsItemControlsItem = {
  /** Unique identifier for the template variable. */
  variableId?: string;
  /** Unique identifier for the control. */
  id?: string;
  /** Human-readable label for the control. */
  label?: string;
  /** Type of the control. */
  type?: JobStatusOutputElementsItemControlsItemType;
  /** Default or current values for the control (e.g., text, fontName, selectedCheckboxValue, selectedDropdownValue, selectedSliderValue, scale). */
  defaultData?: JobStatusOutputElementsItemControlsItemDefaultData;
  /** Data associated with the control. */
  data?: JobStatusOutputElementsItemControlsItemData;
  /** Available options (object for dropdown; array for checkbox). */
  options?: unknown;
  /** Minimum and maximum values for slider controls. */
  range?: JobStatusOutputElementsItemControlsItemRange;
  /** Dimensions for media controls. */
  size?: JobStatusOutputElementsItemControlsItemSize;
  /** Possible scale values for media controls. */
  possibleScaleValues?: string[];
  /** Duration in seconds for audio controls. */
  durationInSeconds?: number;
  /** Possible audio preferences (e.g., replace, mix) for audio controls. */
  possibleAudioPreferences?: string[];
  /** Comment text for comment controls. */
  text?: string;
};

export type JobStatusOutputElementsItem = {
  /** Unique identifier for the element. */
  id?: string;
  /** Type of the element. */
  type: JobStatusOutputElementsItemType;
  /** Array of controls from the template. */
  controls: JobStatusOutputElementsItemControlsItem[];
};

/**
 * Output for describe API jobs.
 */
export type JobStatusOutput = {
  /** Array of fonts from the template. */
  fonts?: JobStatusOutputFontsItem[];
  /** Array of elements from the template. */
  elements?: JobStatusOutputElementsItem[];
};

export interface JobStatus {
  /** ID of the render job. */
  jobId: string;
  /** Current status of the job. */
  status: JobStatusStatus;
  /** Error message when job status is failed. */
  message?: string;
  /** Percentage of job completed, as a string. */
  percentCompleted?: string;
  /** Array of output files with destination URLs, keyed by variation and preset indices. */
  outputs?: JobStatusOutputsItem[];
  /** Array of errors for failed variations (only present when status is partially_succeeded). */
  errors?: JobStatusErrorsItem[];
  /** Output for describe API jobs. */
  output?: JobStatusOutput;
}

/**
 * The media type of the overlay.
 */
export type OverlayMediaType = (typeof OverlayMediaType)[keyof typeof OverlayMediaType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OverlayMediaType = {
  'image/gif': 'image/gif',
  'image/png': 'image/png',
  'image/jpeg': 'image/jpeg',
} as const;

/**
 * Details of the source including a pre-signed URL. See the [list of allowed domains for storage](https://developer.adobe.com/audio-video-firefly-services/getting-started/storage-solutions/).
 */
export type OverlaySource = {
  /** The pre-signed URL pointing to the overlay source. */
  url: string;
};

/**
 * The scale of the overlay.
 */
export type OverlayScale = {
  /** The width of the overlay. */
  width: number;
  /** The height of the overlay. */
  height: number;
};

/**
 * The anchor point of the overlay.
 */
export type OverlayPositionAnchorPoint =
  (typeof OverlayPositionAnchorPoint)[keyof typeof OverlayPositionAnchorPoint];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OverlayPositionAnchorPoint = {
  top_left: 'top_left',
  top_right: 'top_right',
  bottom_left: 'bottom_left',
  bottom_right: 'bottom_right',
  center: 'center',
} as const;

/**
 * The position of the overlay.
 */
export type OverlayPosition = {
  /** The anchor point of the overlay. */
  anchorPoint: OverlayPositionAnchorPoint;
  /** The X offset of the overlay from the anchor point. */
  offsetX: number;
  /** The Y offset of the overlay from the anchor point. */
  offsetY: number;
};

/**
 * The repeat behavior of the overlay.
 */
export type OverlayRepeat = (typeof OverlayRepeat)[keyof typeof OverlayRepeat];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OverlayRepeat = {
  loop: 'loop',
  once: 'once',
} as const;

/**
 * Configuration for an overlay in the reframed video.
 */
export interface Overlay {
  /** The media type of the overlay. */
  mediaType: OverlayMediaType;
  /** Details of the source including a pre-signed URL. See the [list of allowed domains for storage](https://developer.adobe.com/audio-video-firefly-services/getting-started/storage-solutions/). */
  source: OverlaySource;
  /** The start time of the overlay in the video. */
  startTime: string;
  /** The duration of the overlay in the video. */
  duration: string;
  /** The scale of the overlay. */
  scale: OverlayScale;
  /** The position of the overlay. */
  position: OverlayPosition;
  /** The repeat behavior of the overlay. */
  repeat?: OverlayRepeat;
}

/**
 * Error response for the reframed video job.
 */
export interface GenerateReframedVideoJobErrorBody {
  /** A code representing the specific error type. */
  error_code: string;
  /** A description of the error that occurred. */
  message: string;
}

/**
 * Transcribe request body.
 */
export type TranscribeRequest =
  | (TranscribeAudioInput & {
      /** This determines the language used in the processed output. See the usage notes for more about [the supported languages](https://developer.adobe.com/audio-video-firefly-services/getting-started/usage/#language-support). */
      targetLocaleCodes?: TargetLocaleCodes[];
      /** Details to generate captions. */
      captions?: Captions;
    })
  | (TranscribeVideoInput & {
      /** This determines the language used in the processed output. See the usage notes for more about [the supported languages](https://developer.adobe.com/audio-video-firefly-services/getting-started/usage/#language-support). */
      targetLocaleCodes?: TargetLocaleCodes[];
      /** Details to generate captions. */
      captions?: Captions;
    });

/**
 * The request body for transcribing audio.
 */
export interface TranscribeAudioInput {
  audio: AudioInput;
}

/**
 * The request body for transcribing video.
 */
export interface TranscribeVideoInput {
  video: VideoInput;
}

export interface Captions {
  /** Accepted values: ['SRT'] */
  targetFormats?: TargetCaptionFormats[];
}

/**
 * Source details including a pre-signed URL for accessing the input media. See the [list of allowed domains for storage](https://developer.adobe.com/audio-video-firefly-services/getting-started/storage-solutions/).
 */
export interface Source {
  /** The pre-signed URL pointing to the input source. */
  url: string;
}

/**
 * Destination details including a pre-signed URL for the output media.
 */
export interface Destination {
  /** The pre-signed URL pointing to the output destination. */
  url: string;
}

/**
 * The dub request body.
 */
export type DubRequest = DubAudioRequest | DubVideoRequest;

/**
 * The audio dub request body.
 */
export interface DubAudioRequest {
  /** This determines the language used in the processed output. See the usage notes for more about [the supported languages](https://developer.adobe.com/audio-video-firefly-services/getting-started/usage/#language-support). */
  targetLocaleCodes?: TargetLocaleCodes[];
  transcripts?: TranscriptInput[];
  audio: AudioInput;
}

/**
 * The video dub request body.
 */
export interface DubVideoRequest {
  /** This determines the language used in the processed output. See the usage notes for more about [the supported languages](https://developer.adobe.com/audio-video-firefly-services/getting-started/usage/#language-support). */
  targetLocaleCodes?: TargetLocaleCodes[];
  transcripts?: TranscriptInput[];
  video: VideoInput;
  /** If `true`, a high-quality composited lip sync will be used in the video output. */
  lipSync?: boolean;
}

/**
 * Job status response.
 */
export interface JobStatusLinkResponse {
  /** URL to download the results of the job. */
  statusUrl: string;
  /** The job ID. */
  jobId: string;
}

/**
 * Status of the job.
 */
export type FireflyJobApiResponseStatus =
  (typeof FireflyJobApiResponseStatus)[keyof typeof FireflyJobApiResponseStatus];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FireflyJobApiResponseStatus = {
  pending: 'pending',
  running: 'running',
  failed: 'failed',
  succeeded: 'succeeded',
} as const;

/**
 * Firefly job response.
 */
export type FireflyJobResponse = FireflyJobApiResponse | FireflyJobErrorResponse;

export type FireflyJobApiResponseOutputsItem = DubJobResponse | TranscribeOutput;

/**
 * Firefly job success response.
 */
export interface FireflyJobApiResponse {
  status: FireflyJobApiResponseStatus;
  /** Associated job ID. */
  jobId: string;
  /** Job response details. */
  outputs?: FireflyJobApiResponseOutputsItem[];
}

/**
 * Firefly job error response.
 */
export interface FireflyJobErrorResponse {
  status: FireflyJobApiResponseStatus;
  /** Associated job ID. */
  jobId: string;
  /** The error message. */
  message?: string;
  /** Associated error code. */
  error_code?: string;
}

/**
 * The error response object.
 */
export interface ErrorBody {
  /** The error message. */
  message: string;
  /** Associated error code. */
  error_code: string;
}

export type TranscribeOutputItem = {
  destination?: Destination;
  captions?: CaptionOutput;
  localeCode?: TargetLocaleCodes;
};

/**
 * Transcribe success response
 */
export type TranscribeOutput = TranscribeOutputItem[];

export type CaptionOutputItem = {
  destination?: Destination;
  /** Format of the caption */
  format?: TargetCaptionFormats;
};

/**
 * Captions output
 */
export type CaptionOutput = CaptionOutputItem[];

/**
 * Dubbing success response
 */
export type DubJobResponse = DubSuccess | DubPartialFailure;

/**
 * Dubbing success
 */
export interface DubSuccess {
  audioOutput?: AudioOutput;
  videoOutput?: VideoOutput;
  transcriptOutput?: TranscribeOutput;
}

/**
 * Media type of the audio output.
 */
export type AudioOutputMediaType = (typeof AudioOutputMediaType)[keyof typeof AudioOutputMediaType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AudioOutputMediaType = {
  'audio/wav': 'audio/wav',
} as const;

/**
 * Media type of the video output.
 */
export type VideoOutputMediaType = (typeof VideoOutputMediaType)[keyof typeof VideoOutputMediaType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VideoOutputMediaType = {
  'video/mp4': 'video/mp4',
  'video/mov': 'video/mov',
} as const;

/**
 * Audio output in dubbing response.
 */
export interface AudioOutput {
  mediaType?: AudioOutputMediaType;
  destination?: Destination;
}

/**
 * Video output in dubbing response.
 */
export interface VideoOutput {
  mediaType?: VideoOutputMediaType;
  destination?: Destination;
}

/**
 * Dubbing failure for specific language.
 */
export interface DubPartialFailure {
  error?: ErrorBody;
}

/**
 * Transcript input details for a transcribe request.
 */
export interface TranscriptInput {
  /** The pre-signed URL to download the translated/edited transcripts. See the [list of allowed domains for storage](https://developer.adobe.com/audio-video-firefly-services/getting-started/storage-solutions/). */
  source: Source;
}

/**
 * Locale code for dubbing target.
 */
export type TargetLocaleCodes = (typeof TargetLocaleCodes)[keyof typeof TargetLocaleCodes];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TargetLocaleCodes = {
  'da-DK': 'da-DK',
  'en-IN': 'en-IN',
  'en-GB': 'en-GB',
  'es-AR': 'es-AR',
  'es-419': 'es-419',
  'fr-CA': 'fr-CA',
  'hi-IN': 'hi-IN',
  'ja-JP': 'ja-JP',
  'ko-KR': 'ko-KR',
  'nb-NO': 'nb-NO',
  'nl-NL': 'nl-NL',
  'pt-BR': 'pt-BR',
  'sv-SE': 'sv-SE',
  'zh-CN': 'zh-CN',
  'en-US': 'en-US',
  'es-ES': 'es-ES',
  'de-DE': 'de-DE',
  'fr-FR': 'fr-FR',
  'it-IT': 'it-IT',
  'pt-PT': 'pt-PT',
} as const;

/**
 * Target caption format.
 */
export type TargetCaptionFormats = (typeof TargetCaptionFormats)[keyof typeof TargetCaptionFormats];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TargetCaptionFormats = {
  SRT: 'SRT',
} as const;

/**
 * The audio input details for a dub request.
 */
export interface AudioInput {
  source?: Source;
  mediaType: AudioInputMediaType;
}

export type VideoInputMetadata = {
  dialogTrackNumber?: number;
};

/**
 * The video input details for a dub request.
 */
export interface VideoInput {
  source?: Source;
  mediaType: VideoInputMediaType;
  metadata?: VideoInputMetadata;
}

/**
 * Media type of the audio input.
 */
export type AudioInputMediaType = (typeof AudioInputMediaType)[keyof typeof AudioInputMediaType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AudioInputMediaType = {
  'audio/mp3': 'audio/mp3',
  'audio/wav': 'audio/wav',
  'audio/mpeg': 'audio/mpeg',
  'audio/x-m4a': 'audio/x-m4a',
  'audio/aac': 'audio/aac',
} as const;

/**
 * Media type of the video input.
 */
export type VideoInputMediaType = (typeof VideoInputMediaType)[keyof typeof VideoInputMediaType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VideoInputMediaType = {
  'video/mp4': 'video/mp4',
  'video/mov': 'video/mov',
  'video/quicktime': 'video/quicktime',
  'video/x-msvideo': 'video/x-msvideo',
  'video/x-matroska': 'video/x-matroska',
} as const;

/**
 * Details of the source including a pre-signed URL. See the [list of allowed domains for storage](https://developer.adobe.com/audio-video-firefly-services/getting-started/storage-solutions/).
 */
export type VideoInputV2Source = {
  /** The pre-signed URL pointing to the input source. */
  url: string;
};

/**
 * Video input details for generate reframed video request.
 */
export interface VideoInputV2 {
  /** Details of the source including a pre-signed URL. See the [list of allowed domains for storage](https://developer.adobe.com/audio-video-firefly-services/getting-started/storage-solutions/). */
  source: VideoInputV2Source;
}

/**
 * Analysis configuration for the video processing.
 */
export interface AnalysisV2 {
  /** Applies scene edit detection before reframing. */
  sceneEditDetection?: boolean;
  /**
   * Array of focal point keyword identifiers for reframing. Up to 10 keywords are allowed. Keywords must be alphanumeric characters, spaces, hyphens, or underscores only, with a maximum of 1000 characters per keyword.
   * @maxItems 10
   */
  focalPoints?: string[];
}

/**
 * Repeat behavior for GIF overlay (optional).
 */
export type OverlayV2Repeat = null | RepeatV2;

export interface OverlayV2 {
  source?: Source;
  /** Start time for the overlay in the format HH:MM:SS:FF. */
  startTime?: string;
  /** Duration of the overlay in the format HH:MM:SS:FF. */
  duration?: string;
  scale?: ScaleV2;
  position?: PositionV2;
  /** Repeat behavior for GIF overlay (optional). */
  repeat?: OverlayV2Repeat;
}

/**
 * Anchor point for taking the position reference.
 */
export type PositionV2AnchorPoint =
  (typeof PositionV2AnchorPoint)[keyof typeof PositionV2AnchorPoint];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const PositionV2AnchorPoint = {
  top_left: 'top_left',
  top_right: 'top_right',
  bottom_left: 'bottom_left',
  bottom_right: 'bottom_right',
  center: 'center',
} as const;

/**
 * Horizontal offset for the anchor point.
 */
export type PositionV2OffsetX = number | null;

/**
 * Vertical offset for the anchor point.
 */
export type PositionV2OffsetY = number | null;

/**
 * Position configuration for overlay placement.
 */
export interface PositionV2 {
  /** Anchor point for taking the position reference. */
  anchorPoint?: PositionV2AnchorPoint;
  /** Horizontal offset for the anchor point. */
  offsetX?: PositionV2OffsetX;
  /** Vertical offset for the anchor point. */
  offsetY?: PositionV2OffsetY;
}

/**
 * Defines the repeat behavior of the overlay.
 */
export type RepeatV2 = (typeof RepeatV2)[keyof typeof RepeatV2];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RepeatV2 = {
  stop_on_last_frame: 'stop_on_last_frame',
  loop: 'loop',
  time_stretch: 'time_stretch',
} as const;

/**
 * Scale configuration for overlay dimensions.
 */
export interface ScaleV2 {
  /** The width of the scaled overlay. */
  width?: number;
  /** The height of the scaled overlay. */
  height?: number;
}

/**
 * Aspect ratio specification for video output.
 */
export interface AspectRatioV2 {
  /** AspectRatio x ratio. */
  x: number;
  /** AspectRatio y ratio. */
  y: number;
}

/**
 * Media format for the output video. If only the OTIO file is needed, set this as `none`.
 */
export type OutputFormatV2Media = (typeof OutputFormatV2Media)[keyof typeof OutputFormatV2Media];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OutputFormatV2Media = {
  none: 'none',
  mp4: 'mp4',
  mov: 'mov',
  source: 'source',
} as const;

/**
 * Sidecar format for additional metadata.
 */
export type OutputFormatV2Sidecar =
  (typeof OutputFormatV2Sidecar)[keyof typeof OutputFormatV2Sidecar];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OutputFormatV2Sidecar = {
  json: 'json',
  otio: 'otio',
} as const;

/**
 * Output format configuration. A media property is mandatory when this format object is specified.
 */
export interface OutputFormatV2 {
  /** Media format for the output video. If only the OTIO file is needed, set this as `none`. */
  media: OutputFormatV2Media;
  /** Sidecar format for additional metadata. */
  sidecar: OutputFormatV2Sidecar;
}

/**
 * Configuration for composing elements on the video.
 */
export interface CompositionV2 {
  /** List of overlays to be composed on the video. */
  overlays?: OverlayV2[];
}

export type StatusPathParameters = {
  jobId: string;
};
/**
 * Details of the source including a pre-signed URL. See the [list of allowed domains for storage](https://developer.adobe.com/audio-video-firefly-services/getting-started/storage-solutions/).
 */
export type GenerateReframedVideoBodyVideoSource = {
  /** The pre-signed URL pointing to the input source. */
  url: string;
};

/**
 * Supported video media types.
 */
export type GenerateReframedVideoBodyVideoMediaType =
  (typeof GenerateReframedVideoBodyVideoMediaType)[keyof typeof GenerateReframedVideoBodyVideoMediaType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GenerateReframedVideoBodyVideoMediaType = {
  'video/mp4': 'video/mp4',
  'video/quicktime': 'video/quicktime',
} as const;

/**
 * Video input details.
 */
export type GenerateReframedVideoBodyVideo = {
  /** Details of the source including a pre-signed URL. See the [list of allowed domains for storage](https://developer.adobe.com/audio-video-firefly-services/getting-started/storage-solutions/). */
  source: GenerateReframedVideoBodyVideoSource;
  /** Supported video media types. */
  mediaType: GenerateReframedVideoBodyVideoMediaType;
};

/**
 * Configuration details for the generated reframed video.
 */
export type GenerateReframedVideoBodyOutputConfig = {
  /** A list of aspect ratios for the output video. */
  aspectRatios: string[];
};

export type GenerateReframedVideoBody = {
  /** Video input details. */
  video: GenerateReframedVideoBodyVideo;
  /** Apply Scene Edit Detection before reframing. */
  sceneEditDetection?: boolean;
  /** Configuration details for video overlays. */
  overlays?: Overlay[];
  /** Configuration details for the generated reframed video. */
  outputConfig: GenerateReframedVideoBodyOutputConfig;
};

/**
 * Response indicating that the reframed video job has been successfully submitted.
 */
export type GenerateReframedVideo202 = {
  /** The unique identifier for the job. */
  jobId: string;
  /** The URL to monitor the status of the job. */
  statusUrl: string;
};

export type GenerateReframedVideoV2BodyOutputRenditionsItemOneOf = {
  aspectRatio: AspectRatioV2;
  mediaDestination?: Destination;
  sidecarDestination?: Destination;
};

export type GenerateReframedVideoV2BodyOutputRenditionsItemOneOfTwoResolution = {
  /** Resolution width of the output in pixels. */
  width: number;
  /** Resolution height of the output in pixels. */
  height: number;
};

export type GenerateReframedVideoV2BodyOutputRenditionsItemOneOfTwo = {
  resolution: GenerateReframedVideoV2BodyOutputRenditionsItemOneOfTwoResolution;
  mediaDestination?: Destination;
  sidecarDestination?: Destination;
};

export type GenerateReframedVideoV2BodyOutputRenditionsItem =
  | GenerateReframedVideoV2BodyOutputRenditionsItemOneOf
  | GenerateReframedVideoV2BodyOutputRenditionsItemOneOfTwo;

/**
 * Output configuration details for generating the reframed video.
 */
export type GenerateReframedVideoV2BodyOutput = {
  format?: OutputFormatV2;
  /** Array of output rendition specifications. */
  renditions: GenerateReframedVideoV2BodyOutputRenditionsItem[];
};

export type GenerateReframedVideoV2Body = {
  video: VideoInputV2;
  analysis?: AnalysisV2;
  composition?: CompositionV2;
  /** Output configuration details for generating the reframed video. */
  output: GenerateReframedVideoV2BodyOutput;
};

/**
 * Response indicating that the reframed video job has been successfully submitted.
 */
export type GenerateReframedVideoV2202 = {
  /** The unique identifier for the job. */
  jobId: string;
  /** The URL to monitor the status of the job. */
  statusUrl: string;
};

/**
 * Error response for the reframed video job.
 */
export type GenerateReframedVideoV2400 = {
  /** A code representing the specific error type. */
  error_code: string;
  /** A description of the error that occurred. */
  message: string;
};

/**
 * Error response for the reframed video job.
 */
export type GenerateReframedVideoV2401 = {
  /** A code representing the specific error type. */
  error_code: string;
  /** A description of the error that occurred. */
  message: string;
};

/**
 * Error response for the reframed video job.
 */
export type GenerateReframedVideoV2403 = {
  /** A code representing the specific error type. */
  error_code: string;
  /** A description of the error that occurred. */
  message: string;
};

/**
 * Error response for the reframed video job.
 */
export type GenerateReframedVideoV2404 = {
  /** A code representing the specific error type. */
  error_code: string;
  /** A description of the error that occurred. */
  message: string;
};

/**
 * Error response for the reframed video job.
 */
export type GenerateReframedVideoV2405 = {
  /** A code representing the specific error type. */
  error_code: string;
  /** A description of the error that occurred. */
  message: string;
};

/**
 * Error response for the reframed video job.
 */
export type GenerateReframedVideoV2429 = {
  /** A code representing the specific error type. */
  error_code: string;
  /** A description of the error that occurred. */
  message: string;
};

/**
 * Error response for the reframed video job.
 */
export type GenerateReframedVideoV2500 = {
  /** A code representing the specific error type. */
  error_code: string;
  /** A description of the error that occurred. */
  message: string;
};

export type JobResultV2PathParameters = {
  jobId: string;
};
/**
 * The status of the reframed video job.
 */
export type JobResultV2200OneOfStatus =
  (typeof JobResultV2200OneOfStatus)[keyof typeof JobResultV2200OneOfStatus];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JobResultV2200OneOfStatus = {
  not_started: 'not_started',
  running: 'running',
  failed: 'failed',
  succeeded: 'succeeded',
  partially_succeeded: 'partially_succeeded',
} as const;

/**
 * Response indicating that the reframed video job is in progress.
 */
export type JobResultV2200OneOf = {
  /** The status of the reframed video job. */
  status: JobResultV2200OneOfStatus;
  /** The unique identifier for the job. */
  jobId: string;
};

/**
 * The status of the reframed video job.
 */
export type JobResultV2200OneOfThreeStatus =
  (typeof JobResultV2200OneOfThreeStatus)[keyof typeof JobResultV2200OneOfThreeStatus];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JobResultV2200OneOfThreeStatus = {
  not_started: 'not_started',
  running: 'running',
  failed: 'failed',
  succeeded: 'succeeded',
  partially_succeeded: 'partially_succeeded',
} as const;

/**
 * Details of the output destination, including a pre-signed URL.
 */
export type JobResultV2200OneOfThreeOutputsItemOneOfDestination = {
  /** The pre-signed URL pointing to the output. */
  url: string;
};

export type JobResultV2200OneOfThreeOutputsItemOneOf = {
  /** Details of the output destination, including a pre-signed URL. */
  destination: JobResultV2200OneOfThreeOutputsItemOneOfDestination;
};

/**
 * Details of the error encountered during the reframing process.
 */
export type JobResultV2200OneOfThreeOutputsItemOneOfThreeError = {
  /** A code representing the specific error type. */
  error_code: string;
  /** A description of the error that occurred. */
  message: string;
};

/**
 * Error response for the generate reframed video job.
 */
export type JobResultV2200OneOfThreeOutputsItemOneOfThree = {
  /** Details of the error encountered during the reframing process. */
  error: JobResultV2200OneOfThreeOutputsItemOneOfThreeError;
};

export type JobResultV2200OneOfThreeOutputsItem =
  | JobResultV2200OneOfThreeOutputsItemOneOf
  | JobResultV2200OneOfThreeOutputsItemOneOfThree;

/**
 * Success response for the reframed video job.
 */
export type JobResultV2200OneOfThree = {
  /** The status of the reframed video job. */
  status: JobResultV2200OneOfThreeStatus;
  /** The unique identifier for the job. */
  jobId: string;
  /** Array of output rendition results. */
  outputs: JobResultV2200OneOfThreeOutputsItem[];
};

/**
 * The status of the reframed video job.
 */
export type JobResultV2200OneOfOnezeroStatus =
  (typeof JobResultV2200OneOfOnezeroStatus)[keyof typeof JobResultV2200OneOfOnezeroStatus];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JobResultV2200OneOfOnezeroStatus = {
  not_started: 'not_started',
  running: 'running',
  failed: 'failed',
  succeeded: 'succeeded',
  partially_succeeded: 'partially_succeeded',
} as const;

/**
 * Details of the output destination, including a pre-signed URL.
 */
export type JobResultV2200OneOfOnezeroOutputsItemOneOfDestination = {
  /** The pre-signed URL pointing to the output. */
  url: string;
};

/**
 * Success response for the generate reframed video job.
 */
export type JobResultV2200OneOfOnezeroOutputsItemOneOf = {
  /** Details of the output destination, including a pre-signed URL. */
  destination: JobResultV2200OneOfOnezeroOutputsItemOneOfDestination;
};

/**
 * Details of the error encountered during the reframing process.
 */
export type JobResultV2200OneOfOnezeroOutputsItemOneOfThreeError = {
  /** A code representing the specific error type. */
  error_code: string;
  /** A description of the error that occurred. */
  message: string;
};

/**
 * Error response for the generate reframed video job.
 */
export type JobResultV2200OneOfOnezeroOutputsItemOneOfThree = {
  /** Details of the error encountered during the reframing process. */
  error: JobResultV2200OneOfOnezeroOutputsItemOneOfThreeError;
};

export type JobResultV2200OneOfOnezeroOutputsItem =
  | JobResultV2200OneOfOnezeroOutputsItemOneOf
  | JobResultV2200OneOfOnezeroOutputsItemOneOfThree;

/**
 * Response for a reframed video job that partially succeeded.
 */
export type JobResultV2200OneOfOnezero = {
  /** The status of the reframed video job. */
  status: JobResultV2200OneOfOnezeroStatus;
  /** The unique identifier for the job. */
  jobId: string;
  /** Details of the job's partial success, including successful outputs and any errors encountered. */
  outputs: JobResultV2200OneOfOnezeroOutputsItem[];
};

/**
 * The status of the reframed video job, indicating a failure.
 */
export type JobResultV2200OneOfOnesevenStatus =
  (typeof JobResultV2200OneOfOnesevenStatus)[keyof typeof JobResultV2200OneOfOnesevenStatus];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JobResultV2200OneOfOnesevenStatus = {
  not_started: 'not_started',
  running: 'running',
  failed: 'failed',
  succeeded: 'succeeded',
  partially_succeeded: 'partially_succeeded',
} as const;

/**
 * Details of the error encountered during the reframing process.
 */
export type JobResultV2200OneOfOnesevenOutputsRenditionsItemError = {
  /** A code representing the specific error type. */
  error_code: string;
  /** A description of the error that occurred. */
  message: string;
};

/**
 * Error response for the generate reframed video job.
 */
export type JobResultV2200OneOfOnesevenOutputsRenditionsItem = {
  /** Details of the error encountered during the reframing process. */
  error: JobResultV2200OneOfOnesevenOutputsRenditionsItemError;
};

/**
 * Details of the errors encountered during the job.
 */
export type JobResultV2200OneOfOnesevenOutputs = {
  /** Array of failed rendition outputs. */
  renditions: JobResultV2200OneOfOnesevenOutputsRenditionsItem[];
};

/**
 * Error response for a reframed video job that failed.
 */
export type JobResultV2200OneOfOneseven = {
  /** The status of the reframed video job, indicating a failure. */
  status: JobResultV2200OneOfOnesevenStatus;
  /** The unique identifier for the job. */
  jobId: string;
  /** Details of the errors encountered during the job. */
  outputs: JobResultV2200OneOfOnesevenOutputs;
};

/**
 * Job response on status API.
 */
export type JobResultV2200 =
  | JobResultV2200OneOf
  | JobResultV2200OneOfThree
  | JobResultV2200OneOfOnezero
  | JobResultV2200OneOfOneseven;

/**
 * Response indicating that the reframed video job has been successfully submitted.
 */
export type JobResultV2400 = {
  /** The unique identifier for the job. */
  jobId: string;
  /** The URL to monitor the status of the job. */
  statusUrl: string;
};

/**
 * Response indicating that the reframed video job has been successfully submitted.
 */
export type JobResultV2401 = {
  /** The unique identifier for the job. */
  jobId: string;
  /** The URL to monitor the status of the job. */
  statusUrl: string;
};

/**
 * Response indicating that the reframed video job has been successfully submitted.
 */
export type JobResultV2403 = {
  /** The unique identifier for the job. */
  jobId: string;
  /** The URL to monitor the status of the job. */
  statusUrl: string;
};

/**
 * Response indicating that the reframed video job has been successfully submitted.
 */
export type JobResultV2404 = {
  /** The unique identifier for the job. */
  jobId: string;
  /** The URL to monitor the status of the job. */
  statusUrl: string;
};

/**
 * Response indicating that the reframed video job has been successfully submitted.
 */
export type JobResultV2405 = {
  /** The unique identifier for the job. */
  jobId: string;
  /** The URL to monitor the status of the job. */
  statusUrl: string;
};

/**
 * Response indicating that the reframed video job has been successfully submitted.
 */
export type JobResultV2422 = {
  /** The unique identifier for the job. */
  jobId: string;
  /** The URL to monitor the status of the job. */
  statusUrl: string;
};

/**
 * Response indicating that the reframed video job has been successfully submitted.
 */
export type JobResultV2429 = {
  /** The unique identifier for the job. */
  jobId: string;
  /** The URL to monitor the status of the job. */
  statusUrl: string;
};

/**
 * Response indicating that the reframed video job has been successfully submitted.
 */
export type JobResultV2500 = {
  /** The unique identifier for the job. */
  jobId: string;
  /** The URL to monitor the status of the job. */
  statusUrl: string;
};
